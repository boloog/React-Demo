import React from 'react';
import Posts from './Posts';

// 展示组件 通过 props
const App = () => {
  return (
    <div>
      <h1>App</h1>
      <Posts />
    </div>
  );
};

// 类组件 定义行为

export default App;
